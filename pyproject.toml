[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 120

[tool.mypy]
mypy_path = "trubrics"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = 'streamlit.*'
ignore_errors = true
